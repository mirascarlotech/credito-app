class EnvConfig {
  final String chatApiEndpoint;
  final String environment;
  final bool debugMode;

  const EnvConfig({required this.chatApiEndpoint, required this.environment, required this.debugMode});
}
